# AI 同声传译助手 - 环境依赖
# ===========================================
# 运行环境
# ===========================================

# Node.js (必需)
# 下载: https://nodejs.org/
node >= 18.0.0 (推荐 v24.15.0)

# npm (Node.js 自带)
npm >= 9.0.0 (推荐 11.12.1)

# ===========================================
# 浏览器 (必需, Web Speech API + TTS 支持)
# ===========================================

# 语音识别 (Web Speech API) 和语音合成 (Speech Synthesis)
# 仅支持以下浏览器:
Google Chrome >= 25
Microsoft Edge >= 79

# 不支持的浏览器: Firefox, Safari (旧版本), 及其他非 Chromium 浏览器

# ===========================================
# API Key (可选 - 用于翻译功能)
# ===========================================

# DeepSeek API
# 注册: https://platform.deepseek.com/api-docs
# 模型: deepseek-chat

# OpenAI API
# 注册: https://platform.openai.com/api-keys
# 模型: gpt-4o-mini / gpt-4o

# ===========================================
# npm 依赖 (自动安装)
# ===========================================

# 运行时依赖
express ^4.21.0        # HTTP 服务器
ws ^8.18.0             # WebSocket 实时通信
openai ^4.73.0         # OpenAI / DeepSeek API 客户端
dotenv ^16.4.7         # 环境变量加载

# 开发依赖
jest ^29.7.0           # 单元测试框架
